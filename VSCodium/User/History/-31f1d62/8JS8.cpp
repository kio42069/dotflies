#include <bits/stdc++.h>
using namespace std;

    int maxProfit(vector<int>& prices) {
        
    }


int main(){
    vector<int> prices = {};
    int a = maxProfit(prices);
    cout << a << endl;
    return 0;
}
